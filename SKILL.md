---
name: reference-driven-3d-modeling
description: 根据参考图创建或修正风格化 3D 模型时使用。涵盖真实渲染、多视角对比、自主迭代、相互冲突的 2D 参考处理，以及模型和场景的交付验证。适用于 Three.js、Blender 和程序化建模。
---

# 参考图驱动的 3D 建模与视觉迭代

在用户授权范围内，依据视觉参考制作可从多个角度查看的 3D 模型。目标不仅是某张渲染图顺眼，还包括角色特征接近参考、体积在其他角度成立，以及交付物能够重新打开。

本 Skill 是工具中立的单文件能力定义，不依赖特定 Agent、私有服务或固定目录。可使用代码执行、网页检索、图像读取和渲染工具，自主选择 Three.js、Blender、SDF/CSG 或其他建模方法。缺少视觉读取能力时，不能声称完成了视觉验收。

## 1. 核心判断

**形体先于表面修饰。** 识别度首先取决于轮廓、特征点比例、体积和姿态。着色器、毛发、噪声纹理不能修复错误的前肢、眼睛比例或鸭嘴轮廓。先判断问题在哪一层，再修改那一层。

**明确风格取向。** 实物摄影感、游戏角色感和日漫插画感不是同一个目标。风格化不等于减少写实细节，也不等于套用一个 toon shader。用户已给出方向时直接承接，不重复提问；未说明且会影响方案时，先做小样或提出必要的澄清。

**主视角接近不等于 3D 成立。** 需要检查正面、侧面、背面、三分之四等 3–4 个关键视角。用户指定每视角两至三轮时，要逐轮渲染、读图、修改，不能一次改完后把检查点数量算成审查轮数。

只有三项顺序依赖需要固定：参考与相机基线先于塑形；真实渲染先于视觉审查；修改后回看其他角度。其余执行方式由 Agent 根据成本和问题选择。

## 2. 参考治理与冲突处理

### 主参考与来源

选择符合目标、出处可信的图作为主参考，不仅按分辨率高低选择。为每张参考保留来源 URL、本地副本、版本或出处、视角、姿态、可信度，以及它约束哪些部位。

区分官方原画、官方游戏资产的第三方镜像、第三方线稿和来历不明的扫描，不得都称为官方。搜索结果中的图片描述可能误认角色；必须真正打开图，核对主体后再纳入参考池。

公开可浏览不等于可再分发。公开交付只能包含有权发布的文档、代码和资产，不默认把受版权保护的参考原图或模型打包进公共仓库。

### 多张 2D 图不一定对应同一个 3D 模型

先区分相机投影、姿态、风格版本和画法夸张，再判断形体冲突。正面与侧面嘴部长度不同，未必是建模错误；抬脚、弯尾和站立姿态也不能直接做逐像素比较。

跨多张高可信参考重复出现的结构作为优先约束；单张低可信线稿的细节或特定画法作为软约束。无法同时满足时，记录冲突、选择依据和保留的偏差，优先保证共享体积自洽。

可以使用明确命名的姿态或表情变体，但必须保存配置并说明差别。相机切换本身不能触发解剖网格变化；不能为正面、侧面各做一个不同模型，再声称它们是同一个模型的三视图。姿态变体也不是任意改变肢体尺寸的理由。

## 3. 标定与对比

### 先排除相机问题

对每张参考确定近似投影类型、方位、俯仰角、目标中心和姿态。不要为了脚蹼在近水平相机下看不见，就把脚做厚。相机完成初步标定后冻结；需要重新标定时记录依据，不能每轮移动相机来掩盖模型误差。

形体审查使用无背景或中性色图，另留成品场景检查最终表现。比较时允许裁掉空白和标注，并做等比缩放与平移；不允许非等比拉伸参考图迎合模型。保留裁剪范围、对齐规则、相机、姿态和截图的对应关系。

### 数值辅助，不代替读图

包围框长宽比接近、IoU 提高或像素差下降，都不能单独证明还原成功。阴影、文字、基准线、尾巴和姿态差异可能污染数值。要同时看语义特征点、轮廓转折、遮挡关系和空间体积连续性。

启用辅助审查 Agent 时，主 Agent 仍须亲自读回关键图像。审查报告描述了图里不存在的嘴形、手势或颜色时，该结论不能采用；不要把多个模型的口头同意当成视觉证据。

## 4. 有效迭代

每轮形成一个可核对的闭环：真实渲染目标视角，读图并与参考并排或叠图比较，定位 1–3 个具体差异，实施小范围修改，再渲染目标和其他关联视角。后一次渲染也可作为下一轮的比较基线，但不能省略修改后的验证。

差异描述应可定位，例如“嘴根侧面出现硬切面”或“后脑与躯干之间有体积断层”，而不是只有“更胖”“更短”“更精细”。归因至少区分以下层次：

| 层次 | 常见检查对象 |
|---|---|
| 相机 | 投影方式、俯仰、方位、注视点和视野 |
| 姿态 | 肢体摆角、重心、表情和遮挡 |
| 形体 | 控制点、截面、连接处、融合范围和轮廓 |
| 材质 | 基础颜色、粗糙度、描边和明暗分层 |
| 渲染 | 灯光、阴影偏置、抗锯齿和后处理链 |

修改侧面嘴部厚度后，要回看正面和三分之四视角；修正正面头身关系后，要看侧背面。出现明显回归时回到自己保存的有效检查点，不覆盖或恢复无关的用户改动。

### 状态与停止条件

可运行、主视角接近、多视角结构成立、符合风格、用户接受，是不同状态。Agent 自检不能代替用户明确确认。

轮数是预算，不是质量证据。达到上限但仍有差异时，交付当前可用版本和剩余问题，不自动宣称完全还原。图像一致性检查与运行检查应分别记录，不能用无报错来替代像不像。

### 轻量记录示例

下面是格式示例，不是某次任务的验收数据。保留足够的信息支持复查即可，无需引入庞大协议。

```json
{
  "iteration": 2,
  "view": "side",
  "pose": "neutral",
  "reference": "ref_side_01",
  "camera": "side_camera_v1",
  "observed": ["嘴根呈硬切面，背面连接处不连续"],
  "layer": "geometry",
  "change": "调整连接处截面，保持正面宽度不变",
  "regression": {"front": "checked", "back": "checked", "quarter": "checked"},
  "artifacts": {
    "before": "output/02_before.png",
    "after": "output/02_after.png",
    "comparison": "output/02_comparison.png"
  },
  "remaining": ["参考线稿的局部夸张未强行复制"]
}
```

## 5. 已验证的工程陷阱

**正面分体，背面插接。** 为制造正面的头身界线而叠放完整头壳，可能在侧背面形成插接领口。用连续几何解决体积连接，再用受控折痕或描线表达风格；不要以遮挡掩盖断裂。

**细节只在正面成立。** 共面的三根头毛从侧面可能几乎重叠。应给细节可信的空间布局，而不是让部件自动朝向相机来冒充真实形体。若项目确实采用视角相关风格化技术，应明确说明它不是固定几何还原。

**鼻孔和折痕悬浮。** 凭估计放置表面细节容易悬空或埋入网格。可从合适方向向真实表面发射射线，在命中点附加小偏移，并用侧视图验证。射线方向与命中后的法线偏移不是同一件事。

**法线、绕序与局部精度。** 非均匀缩放需要正确的逆转置法线变换；直接修改顶点还要更新相关法线与包围体。错误绕序或剔除设置可能让部件只剩反壳描边。小指尖锯齿化时先检查局部采样密度，不要通过整体平滑抹掉所有特征。

**渲染就绪但画面空白。** Three.js 后处理链即使设置了 ready 标志、没有 pageerror，也可能因缺少真正绘制场景的 RenderPass 而输出空白。要读回截图，必要时检查像素内容，而不只检查退出码。

**软件 WebGL 超时。** 高频重绘密集毛发和阴影可能非常慢，即使机器本身有 GPU，也可能因启动参数使用了软件渲染。静态审查优先按需渲染，并等待明确的完成条件；先查错误和瓶颈，再增加等待时间。

**GLB 字节数组回传。** 把大型二进制转换成巨大 JSON 数组，会产生明显的序列化和传输开销。优先使用 Blob 下载或二进制路径。导出后重新加载，检查几何、纹理和 NaN、Infinity 等异常值。

**场景风格不等于通用资产保真。** 自定义赛璐璐 shader、后处理和反壳描边通常不能直接存入标准 glTF/GLB PBR 材质。分别说明可移植的几何、颜色与贴图，以及需要原引擎场景才能复现的效果。场景截图不能证明 GLB 在其他软件中具有同样外观。

## 6. 交付与验收

按用户范围交付可运行源码或模型、请求的清晰渲染图、多视角图，以及必要的检查点和差异记录。默认不新增仓库、scaffolding、发布任务或目录体系；用户只要一个 Markdown Skill 时，就只交付这个文件。

交付前分别确认：

- 已实际打开参考图，并记录来源、用途和可信度。
- 主视角的轮廓、比例和特征点经过比较，而非只凭记忆建模。
- 其他视角没有明显悬浮、断裂、插接式连接、严重自穿插或异常法线表现。
- 每轮确有修改前后的图像证据，其他视角的回归结果和剩余冲突已经记录。
- 执行了任务范围内的构建、运行、交互和导出重载验证；未执行的检查明确标注。
- 说明哪些纹理和风格效果保留在模型中，哪些依赖原场景。
- 不承诺没有测量依据的相似度百分比，不把预算耗尽写成用户已经接受。

参考不足时补找来源，或把当前结果标为近似；读图工具不可用时只报告运行和数据检查，暂停视觉验收。参考之间无法同时满足时保留冲突，不伪造统一设定。需要外部发布、付费资产或用户独有信息时，停在相应授权边界。

最后以实际成图、可打开的文件和剩余差异说明结果，不以工具调用次数或迭代轮数自证质量。
