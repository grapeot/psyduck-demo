# 摄像头驱动可达鸭：独立设计审查

## 审查结论

**通过，允许进入第一版实现。** `interaction_design.md` 已经选定正确的实现方向，也把范围收在桌面单人上半身 MVP：先取得可重载的蒙皮模型，再用 MediaPipe 上半身关键点驱动角色意图；推理优先进入 Worker，必要时降级到主线程；预设动作、相机失败降级、本地处理和 Pages 子路径构建都纳入验收。没有需要退回设计阶段的阻塞项。

静态源码支持设计对现状的判断：模型统一由 `THREE.Mesh` 创建（`anime.js:126-137`）；主表面和反壳描边的顶点着色器直接使用原始 `position` / `normal`，没有蒙皮变换（`anime.js:55-69`、`anime.js:113-124`）；`neutral` 分支没有把前肢从抱头路径移到独立绑定姿态（`anime.js:8`、`anime.js:197-217`）。当前入口还使用根路径 `/anime.js`，依赖中也尚无 MediaPipe（`index.html:9`、`package.json:5-6`），因此 M4 是待实现目标，不是当前已具备能力。

本次只做静态审查，没有运行构建、模型、摄像头或真人动作测试。设计中的帧率、延迟、跟随稳定性、Worker 兼容性、隐私网络检查和 Pages 子路径就绪状态仍然都是待验收目标，不能据本文称为已测事实。

## 三个承重实现闸门

### 1. 绑定姿态、骨权重和渲染链必须作为同一个资产问题交付

设计已经排除了在现有抱头 GLB 上只加骨骼节点的错误路线。实现时仍要守住一个边界：绑定姿态不是一个仅供展示的 `neutral` 动作，而是网格、骨轴、逆绑定矩阵和后续局部旋转共同依赖的资产基准。应从展开或自然下垂的明确姿态生成或预建前肢几何，导出实际的 `skinIndex`、`skinWeight`、骨架及绑定信息，再由页面重新加载该 GLB。不能先把旧抱头网格烘焙成当前形状，再把“回到 neutral”当作绑定完成。

现有导出路径使用 `duck.clone(true)`（`anime.js:559-580`）。普通 `Object3D.clone()` 不能作为复制复杂蒙皮层级的可靠方案；实现可直接导出专用资产场景，或使用能正确重建骨骼引用的克隆方式，但最终证据必须来自导出后的重新加载，而不是内存中的生成对象。

主表面、反壳描边、法线和投影阴影都要读取同一组蒙皮变换。仅把主体改成 `SkinnedMesh`，却让现有普通 `Mesh` 描边子对象继续使用未变形几何，仍会出现描边脱离。眼睛、瞳孔、鸭嘴和头毛可以刚性挂在头骨下，不需要为这些部件制造多余软权重。

这一闸门的实现验收应包括：重载后确实存在 `SkinnedMesh`、骨架和有限且归一化的权重；逐个驱动头、躯干和左右前肢相关骨骼时，重载资产仍会变形；预设的待机、抬臂、张臂、抱头和歪头都走同一套骨骼控制；正面、侧面和三分之四视角没有明显接缝、穿头、拉颈或描边脱壳。结构检查只能证明蒙皮数据存在，不能替代这些视觉验收。

### 2. 左右、坐标和低置信处理需要先收敛成一个输入契约

检测输入应保持未镜像，MediaPipe 的解剖左右标签也应一直保留；相机小窗只做视觉镜像。角色映射再单独满足屏幕行为：用户在镜像预览右侧抬手时，正面朝向用户的可达鸭也在屏幕右侧抬前肢。由于正面角色的解剖左右与观看者左右相反，实现不能用 CSS 是否镜像来猜骨骼索引，应写出一张固定的“MediaPipe 解剖侧 -> 角色骨骼 -> 屏幕侧”映射，并用左右单臂样例验证。

同一个适配层还要明确 MediaPipe 图像归一化坐标、世界关键点和 Three.js 局部骨骼坐标各自承担什么。二维抱头距离可使用图像坐标并按有效肩宽归一化；肢体方向和躯干姿态转换到校准后的躯干坐标系；角色最终只接收父骨骼局部旋转。不要把图像 `y` 向下、单目 `z` 或髋部世界原点直接当成 Three.js 舞台坐标，也不要用它们移动角色根节点。

置信度判断应按动作所需关键点和单侧肢体执行，而不是只设置一个整帧总阈值。至少处理肩、肘、腕及头部所用关键点的可见度或存在度、非有限值和退化肩宽：单腕遮挡不应让另一侧或躯干突然失效；校准的约两秒必须由连续有效帧构成；短暂丢失保持最后稳定值，超时后混合回待机；重新入画先重新确认，并重置会造成跳变的滤波历史。具体阈值可以在实现中调参，不必扩大为大面积遮挡或精确接触问题。

这一闸门的确定性验收应使用带时间戳的合成回放覆盖：左右单臂、双臂张开、抱头进入与退出滞回、头部侧倾、单侧腕或肘低置信、整人丢失、重新入画、肩宽为零以及 `NaN` 输入。测试既要断言屏幕侧正确，也要断言骨骼角度有限、不会突然翻转，且下肢关键点不会意外开启全身跟随。

### 3. 停止语义、单帧在途和 Pages 资源必须共用一次会话生命周期

建议把浏览器职责边界固定下来：主线程拥有 `getUserMedia`、`video`、帧获取和 UI；Worker 拥有 PoseLandmarker 与 `detectForVideo()`。每次开始或重新开始产生新的会话代号，权限、模型初始化、`ImageBitmap` 创建、Worker 回复和主线程 fallback 的每个异步返回都要先核对代号。停止时先使旧代号失效并停止调度，再关闭全部视频轨道、关闭已取得的图片帧，并关闭或直接终止 Worker。这样旧请求晚到时只能清理自身，不能恢复相机或覆盖新会话状态。

这里需要按 Web API 的真实能力解释设计中的“终止推理”：等待中的 `getUserMedia()` 权限提示本身不能由页面强制取消，晚到的流只能立即关闭；主线程 fallback 中已经进入的同步 `detectForVideo()` 也不能中途抢占，只能在返回后丢弃结果并关闭实例。验收应检查停止操作立即失效旧会话并关闭已经取得的轨道，而不是要求浏览器取消一个不支持取消的底层调用。Worker fallback 也只能在旧 Worker 已终止后启动，不能让两套检测器并发回写。

“单帧在途”应覆盖帧获取、传输、推理和结果消费的完整链路，而不只是 `detectForVideo()` 调用；任一路径成功、失败或取消都要 `close()` 可释放的 `ImageBitmap`。显示循环可以持续运行，但不得排队积压旧检测帧。结果消息应携带会话代号和单调时间戳，避免旧帧在新帧之后应用。

Worker 入口应使用 Vite 可重写的模块 URL；模型和 MediaPipe WASM 文件应固定版本、同站托管，并通过构建基路径解析，不能写死 `/...`，也不能把开发服务器或 CDN 的成功误当成 Pages 子路径成功。实现验收至少要用一个非根前缀构建并挂载 `dist`，直接刷新该前缀下页面，确认 Worker、WASM、模型、GLB 和 JS 全部从预期静态路径加载。网络审计应列出实际请求并确认没有帧或关键点外传，而不是仅根据架构意图宣称隐私通过。

## Mock 与真人验收边界

自动化与 mock 可以验收：预设动作在无相机时可用；合成关键点的动作映射、镜像和遮挡状态机；模拟权限拒绝、模型失败、停止、加载中取消、晚到流和过期 Worker 结果；同一时刻至多一帧在途；蒙皮 GLB 导出重载；生产构建在非根路径加载全部静态资源；请求清单不出现设计外的上传端点。

mock 不能证明 MediaPipe 在目标桌面浏览器中的 Worker/GPU 组合可用，也不能证明真人镜像感受、遮挡恢复、动作自然度、设备帧率或端到端延迟。第一版在 mock 通过后仍需一次明确授权的真人桌面测试，分别记录浏览器、设备、分辨率、Worker 或 fallback 路径，以及左右单臂、张臂、抱头、歪头、遮挡、出画重入和停止后的实际表现。未完成该测试时，只能表述为自动化路径通过，不能表述为真实追踪效果已通过。
