# 摄像头驱动可达鸭：交互设计

你抬手、歪头，可达鸭跟着做；识别与绘制都在浏览器运行，GitHub Pages 只负责托管文件。主要新增工作是把模型变成能自然弯曲的角色，并把人的动作转换成短前肢能完成的动作，而不是更换渲染器。

本文先定义设计，再作为实现与审查依据。当前已获准实现桌面端上半身第一版，完成测试后本地提交并审查；不包含远端建仓、push 或 Pages 发布。全身跟随、精细手指、表情和移动端专项优化留到后续。

## 一、可行性与工程缺口

现有项目基于 Vite + Three.js，静态渲染与 GLB 导出已完成，但动作跟随尚未实现。现有 [模型代码](anime.js) 有三处缺口：

1. 部件由 `THREE.Mesh` 创建，没有骨架及顶点受哪些骨骼影响的权重。需要为可弯曲部分建立 `SkinnedMesh`；刚性五官可作为骨骼子对象随动。
2. 当前 `neutral` 只是双脚站立，前肢仍在抱头，不是便于绑定的标准姿态。不能给这个 GLB 添加几个骨骼节点就认为它能自然弯曲，需要重新建立展开或自然下垂的绑定姿态。
3. 主材质和反壳描边都直接使用原始顶点与法线。身体、描边、法线和阴影要一起支持骨骼变形，不能只有骨架调试线在动。

保留已经校正的头身比例、宽鸭嘴、短鳍状前肢与配色；新模型不得退回通用人形比例。

## 二、第一版怎么使用

初次打开只播放待机动画，不自动请求相机。用户点击开始按钮后加载识别资源，申请摄像头权限，不申请麦克风。双肩和双腕入镜后，面对镜头短暂校准约两秒。

主画面是可达鸭，相机预览放在小窗，可隐藏，也可叠加识别到的骨架供调试。页面始终说明摄像头是否在工作，以及当前是追踪、校准、丢失还是仅有上半身入镜。隐藏预览不等于关闭相机。

停止时先使当前会话失效，停止调度并关闭已取得的视频轨道与 Worker。加载过程中取消也有效：晚到的流立即关闭，过期结果不能更新新会话。浏览器授权弹窗未必可取消，主线程中已开始的同步推理也不能抢占，应在返回后清理和丢弃结果。页面保留重新校准入口。无相机、拒绝授权或模型加载失败时，仍可使用预设动作演示，不显示空白页。

第一版支持单人桌面坐姿或站姿上半身：抬手、张臂、抱头、歪头和左右摆动。跟随时固定三维观察相机；查看模型时才允许自由旋转。默认像照镜子：人在镜像预览哪一侧抬手，鸭子就在屏幕同一侧抬前肢。预览镜像与动作映射分开实现，检测器内部仍保留解剖左右标签，并用左右手测试明确验证。

第二阶段才考虑全身入镜后的有限下蹲与原地踏步。不承诺全屋行走、精确脚底接触、背对相机、大面积遮挡、快速转身或精细手指表情复刻。

## 三、纯浏览器处理流程

```text
摄像头视频帧
  -> 在本机找出肩、肘、腕等位置
  -> 过滤抖动与低可信数据
  -> 转为角色可完成的关节运动
  -> 骨骼带动网格、描边与阴影
  -> Three.js 绘制
```

### 识别与调度

选用 Google 的 MediaPipe Tasks Vision PoseLandmarker。它输出 33 个身体点，包括图像归一化坐标和以髋部中心为原点的三维估计。后者虽然以米为单位，仍是单目模型估计，不是深度实测，也不是人在房间中的绝对位置。[官方 Web 指南](https://developers.google.com/edge/mediapipe/solutions/vision/pose_landmarker/web_js)

Web 端采用 `runningMode: VIDEO`、`numPoses: 1`，关闭不需要的分割掩码。先用 Lite，效果不足再评估 Full，依赖包与配套 WASM、模型文件均固定版本，不使用生产环境的 `latest` CDN。[模型说明](https://developers.google.com/edge/mediapipe/solutions/vision/pose_landmarker)

`detectForVideo()` 同步执行，会阻塞它所在的线程，优先放进 Web Worker。显示循环独立运行；推理只允许一帧在途，不积压旧视频帧。处理完成或取消后显式释放图片帧。Worker 中的 GPU、OffscreenCanvas 和传帧组合需要实测，不预设一定更快；不兼容时允许限频主线程推理并清楚标明降级。[官方线程说明](https://developers.google.com/edge/mediapipe/solutions/vision/pose_landmarker/web_js)

方案不依赖 SharedArrayBuffer、多线程 WASM 或自定义 COOP/COEP 响应头。普通 Worker 与多线程 WASM 是两件事，不能把前者可用当成后者已具备条件。

### 网络与隐私

视频帧和关键点只在本机内存中处理，不上传、不默认录制，不使用云端识别、LLM 或 API 密钥。静态网页、模型和 WASM 的下载仍会产生正常网络请求，因此不宣传完全无网络。优先由同一站点托管运行资源，并用网络检查核实没有帧或关键点外传。

## 四、让模型真正能动

制作单独的可动画资产，例如 `psyduck_rigged.glb`。从标准绑定姿态准备网格、骨轴和权重，不生硬掰开已经烘焙的抱头模型。第一版页面加载预建或轻量生成的可动画资产，不在摄像头运行时重新执行高密度 SDF 建模；无论生成方式如何，都要能导出骨架和权重并重新加载验证。

骨骼数量由需要的自由度决定，大致十几根：根/骨盆、躯干、头颈、左右前肢、脚蹼和尾巴。下肢复杂自由度可以后置，不把固定骨骼数量当验收标准。

可达鸭没有长脖子，头部运动要分摊给头颈与上躯干，并限制幅度，避免拉出细长颈部。眼睛、瞳孔、鸭嘴和头毛随头部刚性附着；肩颈使用平滑权重；前肢保持短圆比例。分段刚性关节可用于最早的动作原型，但第一版展示前要消除明显接缝、拉伸和穿插。

主表面、描边、法线和阴影都要通过相同的骨骼变形链。单独移动骨骼辅助线，或只让主体动而描边留在原地，不算绑定完成。

## 五、模仿动作意图，而非复制坐标

人的手臂比鸭子长，不能每帧拉长角色骨头去追人的手腕。先统一坐标轴、左右和绑定姿态，再在人体躯干坐标系中提取方向、弯曲角度和归一化距离，转换成角色骨骼在父节点中的局部旋转。

抬手和张臂驱动前肢方向；身体侧倾驱动躯干；点头和歪头映射到有限幅度。尾巴可以按身体运动轻微滞后，这是角色附加动画，不是识别出人的尾巴。根节点保留在舞台附近，不从髋部相对坐标推断房间轨迹。

### 抱头动作单独处理

手腕在视频里接近头部，只能提供动作意图，不足以证明真实三维接触。用相对肩宽归一化的二维距离、可见度和持续时间共同判定，低可信深度不能把前肢拉进脑袋。

确认意图后，把角色前肢末端引导到它自己的太阳穴，让中间关节自然弯曲，这部分才交给逆向运动学（IK）。目标应限制在可达范围内，肢长固定，肩肘有旋转限位，并用简化头部碰撞体防止明显穿头。进入和退出使用不同阈值与平滑混合，避免在临界点反复跳动。

角度采用与时间间隔一致的平滑。遮挡时短暂保持稳定值，持续丢失后平缓回到待机，不用低可信猜测突然扭转关节。重新入画先确认有效姿态，再平滑恢复。第一版不因下肢估计存在就自动开启脚部跟随。

## 六、分阶段实现与验收

| 阶段 | 验证内容 | 通过条件 |
|---|---|---|
| M1：无相机动作 | 用预设数据驱动待机、抬臂、张臂、抱头、歪头 | 网格实际变形，表面/描边/阴影一致，无明显接缝或异常拉伸，骨架资产可重载 |
| M2：摄像头骨架 | 独立查看检测点与左右关系，测试拒绝授权和丢失 | 明确区分权限、资源和检测问题；单帧在途；停止及加载中取消都释放相机 |
| M3：上半身跟随 | 合成关键点回放，再做经授权的真人测试 | 镜像、抱头、遮挡、出画重入稳定；跟随与预设演示都可用 |
| M4：静态托管就绪 | 生产构建、子路径预览、资源和网络审计 | 不依赖后端，资源路径正确，不上传帧或关键点；实际发布另行执行 |

本次实现范围为 M1–M3 的上半身版本及 M4 的本地构建就绪，不包含全身动捕和真正的远端部署。自动化测试可使用合成关键点、模拟摄像头与模拟权限；没有运行真人摄像头测试前，不宣称真实追踪效果或设备性能已经通过。

性能设计目标：桌面绘制 30–60 FPS、姿态更新 15–30 Hz，从取得可用视频帧到角色呈现争取约 100–200ms。以上不是实测保证；需要记录具体设备、浏览器和画质，不能用软件时间戳冒充包含传感器曝光的物理端到端延迟。弱设备可降推理频率和渲染分辨率；移动端 Safari 留待专项验证。

## 七、GitHub Pages 与后续发布

沿用 Vite + Three.js，不为本功能引入后端、登录或整套新前端框架。未来由 GitHub Actions 构建 `dist` 并部署 Pages；仓库站点使用 `/repo-name/` 的 base，模型、WASM 和 Worker 都经同一基路径解析，不写死网站根路径。[Vite 部署指南](https://vite.dev/guide/static-deploy)

摄像头要求安全上下文和用户授权，Pages 支持 HTTPS；本地使用 localhost。[MDN 相机权限](https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getUserMedia) · [Pages HTTPS](https://docs.github.com/en/pages/getting-started-with-github-pages/securing-your-github-pages-site-with-https)

代码许可证、宝可梦角色权益、第三方参考图与机器学习模型授权分别处理。未来公开时不默认携带参考原图、私人测试视频或全部历史迭代工件。现在只在现有项目内实现和本地提交，不创建远端仓库，不 push，不发布 Pages。

## 八、设计审查后的实现约束

[独立设计审查](design_review.md) 允许进入实现，提出三个承重闸门：骨架资产与全部渲染链一起验收；左右、坐标及单侧低置信形成确定的输入契约；停止、过期结果和资源寻址共用完整会话生命周期。

实现测试要覆盖零肩宽、非有限关键点、单腕或单肘遮挡、整人丢失与重入。校准时间由连续有效帧累计，不用墙钟时间冒充有效校准。单侧失败不能让另一侧突然失效。复制或导出蒙皮层级时使用能保持骨骼引用的方式，最终以重新加载资产的变形结果为准。

单帧在途包括帧获取、传输、推理和消费；所有成功、失败、取消路径都负责释放图片帧。每次开始产生新会话代号，所有异步结果核对会话和时间戳。生产构建需在实际非根前缀挂载后检查 Worker、WASM、模型、GLB 和 JS 请求，不以开发服务器成功代替。
